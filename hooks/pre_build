#!/bin/bash
echo "=> Building the binary"
echo "$USER"
id -u -n
echo $(pwd)
ls -la
echo "perm"
ls -ld .
sudo docker run --privileged --rm -v $(pwd):/home/gradle/project -w /home/gradle/project gradle:alpine gradle -s build
