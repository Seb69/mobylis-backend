#!/bin/bash
echo "=> Building the binary"
echo "$USER"
id -u -n
mkdirs /home/gradle/project/
echo $(pwd)
ls -la
echo "perm"
ls -ld .
docker run --privileged --rm -v $(pwd):/home/gradle/project -w /home/gradle/project gradle:alpine gradle -s build --debug
